[project]
name = "collabflow"
version = "0.1.0"
description = "Collabflow Backend"
requires-python = ">=3.9"
dependencies = [
    "anthropic",
    "python-dotenv",
    "requests"
]

[tool.vercel]
entrypoint = "backend.server:Handler"
